import React, {FC, ReactElement, ReactNode} from 'react';
import {View, Text} from 'react-native';
import styles from './styles';

interface Props {
  children?: ReactNode | ReactElement;
}

const AAAAAAA: FC<Props> = ({}) => {
  return <View />;
};

export default AAAAAAA;
