import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    position: 'absolute',
    top: 240,
    left: 100,
  },
  image: {
    width: 489,
    height: 225,
    resizeMode: 'contain',
  },
});
