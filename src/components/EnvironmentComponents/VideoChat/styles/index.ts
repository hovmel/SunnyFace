import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    position: 'absolute',
    bottom: 45,
    left: 100,
    zIndex: 102,
  },
  webview: {
    width: 1000,
    height: 450,
    backgroundColor: 'blue',
  },
});
