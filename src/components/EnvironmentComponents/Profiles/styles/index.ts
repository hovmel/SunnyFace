import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    position: 'absolute',
    top: 720,
    right: 0,
  },
  image: {
    width: 768,
    height: 180,
    resizeMode: 'contain',
  },
});
