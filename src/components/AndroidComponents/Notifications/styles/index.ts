import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    position: 'absolute',
    top: 10,
    left: 650,
  },
  image: {
    width: 400,
    height: 286,
    resizeMode: 'contain',
  },
});
