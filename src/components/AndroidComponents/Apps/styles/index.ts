import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    position: 'absolute',
    top: 290,
    left: 50,
  },
  image: {
    width: 1700,
    height: 720,
    resizeMode: 'contain',
  },
});
