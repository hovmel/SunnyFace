import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    position: 'absolute',
    top: 10,
    left: 40,
  },
  image: {
    width: 449,
    height: 920,
    resizeMode: 'contain',
  },
});
